let str2: string = 'abc';

// str = 123;

let tuple: [number, string] = [23, '123'];

let compute: (x: number, y: number) => number;
compute = (a,b) => {return 123}

enum Role {
  Developer
}

console.log(Role.Developer);